# Demo generica de invitaciones web

Demo React/Vite para mostrar el concepto de invitacion web sin asociarlo a un evento puntual.

Edita `src/invitationConfig.js` para cambiar marca, colores, fotos, fecha, lugar, WhatsApp, cronograma y secciones.

```bash
npm install
npm run dev
npm run build
```
