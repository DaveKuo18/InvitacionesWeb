/**
 * @typedef {"boda" | "quince" | "bautismo" | "cumple" | "recibida"} EventType
 * @typedef {"whatsapp" | "form" | "both"} RsvpMode
 * @typedef {"text" | "number" | "email" | "tel" | "select" | "textarea"} RsvpFieldType
 *
 * @typedef {Object} InvitationConfig
 * @property {string} slug
 * @property {"active" | "inactive"} status
 * @property {EventType} eventType
 * @property {string} template
 * @property {string} templateVersion
 * @property {string} title
 * @property {string} eyebrow
 * @property {string} dateISO
 * @property {string} displayDate
 * @property {string} intro
 */

export {};
